# Exact pins: the release binary is built from this file, so it must resolve
# to the same bytes every time. Bump deliberately, then rebuild and smoke test.
watchdog==6.0.0
tinytag==2.2.1
flask==3.1.3
Pillow==12.2.0
